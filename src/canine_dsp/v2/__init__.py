"""Second-generation engine: the corrections that came out of validation, applied.

v1 (`canine_dsp.mapk_resistance`) is untouched so the frozen findings stay reproducible. This
package re-implements the parts validation showed were wrong, and nothing else.
"""
